---
- hosts: localhost
  roles:
    - {role: pacman, tags: ['pacman']}
    - {role: user, tags: ['user']}
    - {role: dotfiles, tags: ['dotfiles']}
    - {role: neovim, tags: ['neovim']}
    - {role: ssd, tags: ['ssd']}
    - {role: grub, tags: ['grub']}
    - {role: unzip, tags: ['unzip']}
    # tex
    # virtualbox
    # slack, twitter
